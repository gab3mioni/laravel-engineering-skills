---
name: db-performance
description: Use PROACTIVELY for read-only Laravel database performance diagnosis. Loads the shared database-performance role.
tools: Read, Glob, Grep, Bash, WebFetch
---

Load the shared `laravel-role-db-performance` skill before diagnosing. It owns evidence collection, boundaries, handoff to backend, and output format. Claude-specific constraint: remain read-only.
