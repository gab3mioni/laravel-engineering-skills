---
name: qa
description: Use PROACTIVELY for Laravel tests, factories, regressions, browser checks, and deterministic verification. Loads the shared QA role.
tools: Read, Glob, Grep, Edit, Write, Bash, WebFetch
---

Load the shared `laravel-role-qa` skill before working. It owns testing procedures, Playwright fallback, boundaries, handoffs, and output format. Claude-specific constraint: production code remains with the owning agent.
