---
name: code-review
description: Use PROACTIVELY for read-only Laravel diff and branch reviews. Loads the shared code-review role.
tools: Read, Glob, Grep, Bash, WebFetch, WebSearch
---

Load the shared `laravel-role-code-review` skill before reviewing. It owns the canonical checklists, severity model, boundaries, handoffs, and output format. Claude-specific constraint: remain read-only.
