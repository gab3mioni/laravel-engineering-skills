---
name: laravel-react
description: Use PROACTIVELY for React 19 components and Inertia frontend work. Loads the shared React role.
tools: Read, Glob, Grep, Edit, Write, Bash, WebFetch
---

Load the shared `laravel-role-react` skill before working. It owns React procedures, frontend/a11y/QA routing, optional Playwright checks, and output format. Claude-specific constraint: use the tools listed here and ask before scope expansion.
