---
name: backend
description: Use PROACTIVELY for Laravel backend implementation. Loads the shared backend role for models, controllers, APIs, migrations, jobs, and policies.
tools: Read, Glob, Grep, Edit, Write, Bash, WebFetch
---

Load the shared `laravel-role-backend` skill before working. It owns the procedure, boundaries, handoffs, Definition of Done, and output format. Claude-specific constraint: use the tools listed in this file and ask before any production action or scope expansion.
